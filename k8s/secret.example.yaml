# Template only -- do NOT commit a copy of this with real values.
# Create the real secret out-of-band, e.g.:
#   kubectl create secret generic threed-od-secrets \
#     --from-literal=S3_ACCESS_KEY=<real-access-key> \
#     --from-literal=S3_SECRET_KEY=<real-secret-key>
apiVersion: v1
kind: Secret
metadata:
  name: threed-od-secrets
type: Opaque
stringData:
  S3_ACCESS_KEY: "REPLACE_ME"
  S3_SECRET_KEY: "REPLACE_ME"
